CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(SORT)

AUX_SOURCE_DIRECTORY(${PROJECT_SOURCE_DIR} SORT_SRC)

ADD_LIBRARY(sort SHARED ${SORT_SRC})
INSTALL(TARGETS sort LIBRARY DESTINATION /usr/lib)
FILE(GLOB HEADERS ${PROJECT_SOURCE_DIR}/*.h)
INSTALL(FILES ${HEADERS} DESTINATION /usr/include)
